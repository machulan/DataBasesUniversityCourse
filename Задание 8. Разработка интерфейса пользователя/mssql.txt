https://habrahabr.ru/post/241079/

SELECT  *
FROM    sys.objects
WHERE   type = 'U';

AF = статистическа€ функци€ (среда CLR);
C = ограничение CHECK;
D = DEFAULT (ограничение или изолированный);
F = ограничение FOREIGN KEY;
PK = ограничение PRIMARY KEY;
P = хранима€ процедура SQL;
PC = хранима€ процедура сборки (среда CLR);
FN = скал€рна€ функци€ SQL;
FS = скал€рна€ функци€ сборки (среда CLR);
FT = возвращающа€ табличное значение функци€ сборки (среда CLR);
R = правило (старый стиль, изолированный);
RF = процедура фильтра репликации;
S = системна€ базова€ таблица;
SN = синоним;
SQ = очередь обслуживани€;
TA = триггер DML сборки (среда CLR);
TR = триггер DML SQL; 
IF = встроенна€ возвращающа€ табличное значение функци€ SQL;
TF = возвращающа€ табличное значение функци€ SQL;
U = таблица (пользовательска€);
UQ = ограничение UNIQUE;
V = представление;
X = расширенна€ хранима€ процедура;
IT = внутренн€€ таблица.